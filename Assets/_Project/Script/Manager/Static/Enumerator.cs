public enum GameTimeType
{
    Pause,
    Normal,
    Accelerate
}

public enum DayTime
{
    Dawn,
    Day,
    Dusk,
    Night
}

public enum StatsRange
{
    Zero,
    Zero_One,
    One,
}

public enum ItemState
{
    New,
    Consumed,
    In,
    Out,
    TurnedOn,
    TurnedOff
}
